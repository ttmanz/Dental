PORT=3001

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=dental_pro
DB_USER=dental_app
DB_PASSWORD=change_me

# JWT — generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=replace_with_random_64_byte_hex
JWT_EXPIRES_IN=8h

# Claude AI (forwarded from frontend — practice sets their own key)
# ANTHROPIC_API_KEY is per-practice in ai_settings, not a server secret
